# api/ (Week 5 stub)

FastAPI backend lives here: wraps the hybrid retriever + Ollama LLM call
behind an HTTP endpoint, enforcing the grounding-in-retrieved-context-only
prompt template. Not implemented yet.
