# api/ (Week 8 stub)

FastAPI backend lives here: wraps `generation/generate_answer.py` (the
hybrid retriever + grounded Ollama generation built in Weeks 4-5) behind an
HTTP endpoint for the Streamlit frontend (`ui/`) to call. Not implemented
yet.
