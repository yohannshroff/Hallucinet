# kg/ (Week 3-4 stub)

Entity resolution and Neo4j ingestion live here: normalizing/fuzzy-matching
entity aliases from `data/kg_seed/entities.csv`, loading
`data/kg_seed/relationships.csv` into Neo4j via Cypher, and any graph
validation queries. Not implemented yet.
